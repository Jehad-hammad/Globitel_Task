

namespace Domain.Enums
{

    public enum RoleTypes
    {
        Admin = 1,
        Client = 2,
    }
    public enum Gender
    {
        Male = 1,
        Female = 2,
    }
    public enum UserStatus
    {
        Active = 1,
        Disabled = 2,
    }
}
